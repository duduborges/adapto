import type { Locale } from '@/types';

export const i18n = {
  defaultLocale: 'en' as Locale,
  locales: ['en', 'pt', 'es'] as Locale[],
};

export const languages = {
  en: 'English',
  pt: 'Português',
  es: 'Español',
};
